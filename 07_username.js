//generate user-name having @ concatenated in front and length of the string in the end
let Fullname=prompt("Enter your full name without spaces");
let username="@"+Fullname+Fullname.length;
console.log(username);
